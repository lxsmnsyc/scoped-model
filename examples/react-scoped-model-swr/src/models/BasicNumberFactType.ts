import { createStateModel } from '@lxsmnsyc/react-scoped-model';

const BasicNumberFactType = createStateModel('trivia', {
  displayName: 'BasicNumberFactType',
});

export default BasicNumberFactType;
