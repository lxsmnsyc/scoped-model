import { createStateModel } from '@lxsmnsyc/react-scoped-model';

const BasicNumberFactNumber = createStateModel('0', {
  displayName: 'BasicNumberFactNumber',
});

export default BasicNumberFactNumber;
